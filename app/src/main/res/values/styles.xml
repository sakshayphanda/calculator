<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ShowCaseViewStyle" parent="ShowcaseView.Light">

        <item name="sv_backgroundColor">@color/showCaseBackground</item>
        <item name="sv_titleTextAppearance">@color/title</item>
        <item name="sv_detailTextAppearance">@color/detail</item>
        <item name="sv_buttonText">Got it</item>

    </style>



</resources>
